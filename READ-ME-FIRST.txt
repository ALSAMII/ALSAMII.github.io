Version 351 · last updated 2026-08-31 10:19 PDT

chewzfiction.com — the Persian on its own line
==============================================
Cache version ?v=351. Seventy-nine stories, twelve series.

This replaces the ?v=350 batch. No new books: nothing in pdfs/,
covers/ or read/ changed, and share/ is unchanged from last time
(it only moves when stories.js does). Upload only what is below.

  script.js    a series that carries Persian now sets the English and
               the Persian as two separate lines - in the heading and
               in the small line under each book's reading time -
               instead of letting the Persian land wherever the
               English happened to run out of column
  style.css    a line each for the two scripts, led for the nastaliq;
               and the series heading no longer clips: on a desktop
               the shelf is a narrow column, so the row of spines
               drops beneath the title rather than squeezing it
  index.html   ?v=351
  feed.xml     rebuilt
  sitemap.xml  rebuilt

Not included, because they have not changed since the last batch:
  stories.js   already uploaded at ?v=350
  share/       already uploaded at ?v=350
Both are still in Downloads/claude if you need them again.

Each file above carries the version line at its top, as a comment in
its own syntax - /* */ in the .js and .css, an HTML comment just under
<meta charset> in index.html, an XML comment under the declaration in
the feed and the sitemap. Comments only; nothing the browser reads.
Re-running build-feeds.js will drop the stamp from feed.xml and
sitemap.xml, since it writes those two from scratch.

Filenames stay as they are, so this folder can be referenced by a
fixed path.

Never delete CNAME.
