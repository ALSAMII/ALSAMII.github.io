chewzfiction.com — five fixes
=============================
Cache version ?v=350. Seventy-nine stories, twelve series.

No new books here. Nothing in pdfs/, covers/ or read/ changed, so
those folders are not included — upload only what is below.

  stories.js   eleven synopses cut to house length (69-79), and the
               Thursday Nights blurb brought up to four accounts
  script.js    series panel clears the single cover; Persian titles
               and labels tagged so the stylesheet can lead them
  style.css    leading for the two-script titles; the feature cover
               top-aligned and no longer wrapping below the synopsis
  index.html   ?v=350
  feed.xml     rebuilt
  sitemap.xml  rebuilt
  share/       the whole folder, rebuilt - 79 pages

Never delete CNAME.
